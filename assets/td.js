/* Website-Skript — lokal, kein CDN, keine Cookies, keine persistenten IDs.
   Sends sind pausiert. Kein fetch, kein sendBeacon, kein POST.
   Datei und Script-Tags bleiben eingebunden. */
(function () {
  /* absichtlich leer — kein fetch, kein sendBeacon, kein POST */
})();
