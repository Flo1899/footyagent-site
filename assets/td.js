/* TelemetryDeck Website — lokal, kein CDN, keine Cookies, keine persistenten IDs.
   Sends sind pausiert, bis eine Website-App-ID im Dashboard indexiert.
   Niemals die iOS-App-ID wiederverwenden: website.* landete sonst in der App-Übersicht.
   Dieses Skript bleibt eingebunden, damit wir später nur hier wieder einschalten. */
(function () {
  /* absichtlich leer — kein fetch, kein sendBeacon, kein POST */
})();
